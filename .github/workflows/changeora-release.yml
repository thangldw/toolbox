name: Release Changeora

on:
  push:
    tags:
      - "changeora-v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: macos-15
    timeout-minutes: 30
    defaults:
      run:
        working-directory: apps/changeora
    steps:
      - uses: actions/checkout@v6
      - name: Validate tag and app version
        if: github.ref_type == 'tag'
        run: |
          TAG_VERSION="${GITHUB_REF_NAME#changeora-v}"
          APP_VERSION="$(/usr/libexec/PlistBuddy -c 'Print :CFBundleShortVersionString' Resources/Info.plist)"
          test "$TAG_VERSION" = "$APP_VERSION"
      - name: Test
        run: ./scripts/test_core.sh
      - name: Build unsigned release
        env:
          CHANGEORA_UNIVERSAL: "1"
        run: ./scripts/build_release.sh
      - name: Publish GitHub Release
        if: github.ref_type == 'tag'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$GITHUB_REF_NAME" \
            release/Changeora-*-unsigned.zip \
            release/Changeora-*-unsigned.zip.sha256 \
            --title "Changeora ${GITHUB_REF_NAME#changeora-v}" \
            --generate-notes \
            --verify-tag
      - name: Upload manual-build artifacts
        if: github.ref_type != 'tag'
        uses: actions/upload-artifact@v7
        with:
          name: changeora-unsigned
          path: apps/changeora/release/*
          if-no-files-found: error
